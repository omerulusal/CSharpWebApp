<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/MoviesApp.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css">
</head>
<body>
    @RenderSection("message",false)
    @await Html.PartialAsync("_navbar")

    @RenderSection("jumbo", false)
    @*
    RenderSection ile jumbo adını verdigim Index.cshtmldeki kodu cagırdım, Amacım Jumbodaki kodun ön tarata olmasını saglamaktı ıkıncı parametre
    olarak false verdim eger false vermeseydim jumboyu kullanmadıgım baska sayfalar patlayacaktı bu yuzden jumbo varsa gosterir yoksa gostermez

    false ın anlamı sectionu baska sayfada tanımlamamını zorunlulugu yok.
    *@



    <div class="container">

        @if (IsSectionDefined("sidebar"))
        {
            @*Eger bir sayfada sidebar adında bir section tanımlanmışsa bu if blogu calışır.*@
            <div class="row">
                <div class="col-md-3">
                    @RenderSection("sidebar", false)
                    @*false: sidebar sectionın her sayfada tanımlanmasına gerek yok *@
                </div>
                <div class="col-md-9">
                    @RenderBody()
                    @*RenderBody ile tum sayfalar buraya eklenir. *@
                </div>
            </div>
        }
        else
        {
            <div class="row">
                <div class="col-md-12">
                    @RenderBody()
                </div>
            </div>
        }

    </div>
    @await Html.PartialAsync("_footer")
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
